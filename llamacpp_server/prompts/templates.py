"""Шаблоны промптов для RAG-only системы."""


# Системный промпт для RAG-only системы
SYSTEM_PROMPT = """Ты помощник для ответов на вопросы на основе предоставленной документации. 

Правила:
- Отвечай на русском языке развернуто и подробно
- Используй только информацию из предоставленного контекста
- НЕ выдумывай информацию, которой нет в документах
- Если в документах нет ответа, честно скажи об этом
- В конце ответа добавь раздел "Источники:" со ссылками на релевантные документы
- Формат источников: "• название_документа - ссылка"
- НЕ упоминай page_url в основном тексте ответа"""

# RAG шаблон для всех запросов
RAG_TEMPLATE = """Контекст из документации:
{context}

Вопрос: {question}

Ответ:"""

# Шаблоны для форматирования чата (оставляем для совместимости)
CHAT_TEMPLATES: dict[str, str] = {
    "llama2": """<s>[INST] <<SYS>>
{system_prompt}
<</SYS>>

{user_message} [/INST]""",
}

# Обратная совместимость
SYSTEM_PROMPTS: dict[str, str] = {
    "default": SYSTEM_PROMPT,
}

RAG_TEMPLATES: dict[str, str] = {
    "citation_focused_prompt": RAG_TEMPLATE,
}
