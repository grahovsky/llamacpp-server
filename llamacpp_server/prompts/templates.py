"""Шаблоны промптов для различных задач."""


# Системные промпты для различных задач
SYSTEM_PROMPTS: dict[str, str] = {
    "default": """Отвечай на русском языке, развернуто и подробно, предоставляй исчерпывающую информацию. Используй только предоставленные документы. НЕ выдумывай.""",
}

# RAG шаблоны для контекстного поиска
RAG_TEMPLATES: dict[str, str] = {

    "citation_focused_prompt": """Отвечай на русском языке, развернуто и подробно, предоставляй исчерпывающую информацию. Используй только предоставленные документы. НЕ выдумывай.

{context}

Вопрос: {question}

ВАЖНО:
1. Дай развернутый ответ на основе документов выше
2. НЕ упоминай page_url в основном тексте ответа
3. В самом конце ответа добавь раздел "Источники:" с перечислением всех page_url из релевантных для ответа документов. Если документ не релевантен, то не указывай его в источниках.
4. Формат источников: "• page_name - page_url"
5. page_url из документации - обязательно должен быть указан в раделе "Источники:"

Ответ:"""
}

# Шаблоны для форматирования чата
CHAT_TEMPLATES: dict[str, str] = {
    "llama2": """<s>[INST] <<SYS>>
{system_prompt}
<</SYS>>

{user_message} [/INST]""",
}
